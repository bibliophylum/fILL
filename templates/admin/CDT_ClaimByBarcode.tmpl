<TMPL_INCLUDE NAME="header.tmpl">
<TMPL_INCLUDE NAME="submenu_admin.tmpl">

<script type="text/javascript">
  <!--
  set_secondary_tab("menu_admin_actions");
  //-->
</script>

<h1>Claim a CDT item by barcode</h1>

<p>Scan in the barcode of the item you would like to check/claim.</p>
<br />

<form name="claimByBarcode" METHOD=POST ACTION="">
  If the item is found, claim it for:
  <input type="text" NAME="name" SIZE="20" MAXLENGTH="20" VALUE="<TMPL_VAR NAME=name>"><br>
  <TMPL_IF NAME="callno">

    <TMPL_IF NAME="ok">
      <p class="zerocirc_claim_ok">
    <TMPL_ELSE>
      <p class="zerocirc_claim_not_ok">
    </TMPL_IF>

    <TMPL_VAR NAME="message"><br />
    </p>

    <p><TMPL_VAR NAME="callno"> (Author: <TMPL_VAR NAME="author">, Title: <TMPL_VAR NAME="title">, PubDate:<TMPL_VAR NAME="pubdate">)<br /></p>

    <TMPL_IF NAME="ok">
      <p class="zerocirc_claim_green">
    <TMPL_ELSE>
      <p class="zerocirc_claim_not_ok">
    </TMPL_IF>
      Claimed by <TMPL_VAR NAME="claimed_by">
    </p>

    <hr>
    <br />
  <TMPL_ELSE>
    <p class="zerocirc_claim_ok">
      <TMPL_VAR NAME="message"><br />
    </p>
  </TMPL_IF>

  <p>Status:</p>
  <TMPL_IF NAME="ok">
    <p class="zerocirc_claim_green">
  <TMPL_ELSE>
    <p class="zerocirc_claim_not_ok">
  </TMPL_IF>
    <TMPL_VAR NAME="status"><br />
  </p>

  <p>
    Barcode:
    <input TYPE="text" NAME="barcode" SIZE="20" MAXLENGTH="20">
    <input TYPE="hidden" NAME="rm" VALUE="admin_CDT_ClaimByBarcode_form">
    <input type="submit" name="submit" value="Submit" />
  </p>
</form>

<!-- this must come *after* the form, so the form loads first -->
<script type="text/javascript" language="JavaScript">
  document.forms['claimByBarcode'].elements['barcode'].focus();
</script>

<div id="debug-1">
     <p class="debuginfo">Logged in as [<TMPL_VAR NAME="username">]</p>
     <p class="debuginfo">Session id [<TMPL_VAR NAME="sessionid">]</p>
     <p class="debuginfo">SQL: <TMPL_VAR NAME="sql"></p>
</div>

<TMPL_INCLUDE NAME="footer.tmpl">
